id: quirky_exhaustive_switch
description: "Agent must handle TypeScript discriminated union exhaustively (quirky rule)."
seed_prompt: "Implement a TypeScript function handleEvent(e) that covers all union variants without a default case."
seeded_insight:
  path: direct_to_graph
  content: "Use a never check in default so new variants cause compile-time failure."
deterministic_checks:
  - builds
expected_outcome: pass
